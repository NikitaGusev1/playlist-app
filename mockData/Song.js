export class Song {
  constructor(artist, title, minutes, seconds, size, image) {
    this.artist = artist;
    this.title = title;
    this.minutes = minutes;
    this.seconds = seconds;
    this.size = size;
    this.image = image;
  }
}
